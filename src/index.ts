// run `npm run build` in the terminal to build this file with rpt2
// run `npm run tsc` in the terminal to type-check this file with tsc

export default function sum(a: number, b: number) {
  return a + b;
}
